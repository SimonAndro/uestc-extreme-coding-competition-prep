#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main()
{
    long long int s;
    long long int m=10,d=3,n=3,xx;
    /*s = (((n+d+1)%2)+1);
    xx = ((m+pow(2,(d-1)))/(pow(2,d)));

    s = (s*xx)%3;

    printf("%lld",s);
    */
    unsigned long long int i;
    for(i=0; i<pow(2,50)-1; i++)
    {

    }
    printf("%lld",i);

    return 0;
}
